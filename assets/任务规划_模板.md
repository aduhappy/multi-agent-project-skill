# 任务规划：<主题>

> 每张任务卡**自包含**：不依赖聊天记录；零上下文 agent 读本卡 + 线级 `AGENTS.md`/`STATUS.md` + 卡内列出的输入即可执行，其余必需信息都写进卡里。
> 关键任务在建卡时同时填写执行方和独立复核方两个提示词；建议模型不是实际运行身份。
> 新项目可将本模板复制为 `文档/任务卡/T1_<主题>.md`；旧项目不要盲目移动历史卡。

## 版本与状态
- **任务卡编号**：T1
- **当前有效版本**：`T1-v1`
- **状态**：🟠待裁定（不可派）/ ⬜未开始 / ⏳进行中 / 🟡待复核 / ✅完成 / ⛔退回 / 🗄️作废
  - **只有 ⬜未开始 可派**；其余状态执行方一律停下回报。卡里还有要主控拍板的问题时标 🟠，裁定后改成 ⬜；退回返修的卡改完也改回 ⬜。
- **状态日期**：YYYY-MM-DD

## 任务定义
- **目标**：【一句话说清要产出什么】
- **卡面数字出处**：卡里写的具体数字都注明来源文件和口径；没核过的标“主控估算”。执行方开工先核对，对不上就停。
- **谁负责**：【待填：执行方 actor；不要只写建议模型】
- **⚙️ 建议模型**（可选）：【待填：可写 Claude / GLM / DeepSeek / MIMO 等候选；不得写成唯一默认】
- **精确输入**：
  - 路径 / URL / DOI：【待填】
  - 输入版本：【待填】
- **精确输出**：
  - 路径、格式、单位：【待填】
  - 输出版本：【待填】
- **环境/解释器**：【待填：环境名、解释器版本、必要变量】
- **注意（坑·铁律）**：
  - 【待填：不可混用维度、只读路径、禁止静默修正】
- **验收**：
  - 【待填：可运行命令、预期汇总行、容差和阈值】
  - 哈希只记判据依赖的对象（交付物、声称未改的输入）；不给每份日志/快照逐个记哈希
- **依赖**：无 / 依赖 T0 / 精确引用上游输出路径
- **已知风险**：【待填；没有就写“无”】
- **判读规则**（可选，研究/分析类推荐）：结果出来前写死“怎样算成立/不成立”，🔴 不得因结果改动；产出先写成候选文件，是否采纳另行裁定。

## 身份（执行方和复核方各写一行，实际运行后填）
- **执行方**：工具【】/ 模型【】/ 家族【】/ 日期【】；家族按模型名写（GPT→OpenAI、Claude→Anthropic、GLM→智谱、DeepSeek→DeepSeek 等），连模型名都不知道才写 UNKNOWN；不从工具名猜
- **复核方**：工具【】/ 模型【】/ 家族【】/ 日期【】；家族写法同上；结论：通过 / 有条件通过 / 退回 / UNKNOWN；报告【路径】

## 交付前必做
- [ ] 验收全过，保留机器可提取的汇总行
- [ ] 需要复核的任务已复核；UNKNOWN 不当作通过
- [ ] 更新线级 `AGENTS.md` §3 / §4 与 `STATUS.md`
- [ ] 新脚本/数据已登记到 `scripts/README.md` / `来源.txt`

## 📋 执行方派发提示词（复制即用）
````text
先看本任务卡 T1 的状态：只有 ⬜未开始 可派，不是就停下回报主控。
再读线级 AGENTS.md（含 STATUS.md），不要依赖聊天上下文。
开工前报一下你的工具、模型、模型家族（家族按模型名写，连模型名都不知道才写 UNKNOWN）。
先核对卡面数字与输入是否对得上，对不上就停。
本次目标：【一句话目标】。
不能动：【原始数据/正式稿/路径】。
输入：【精确路径】；输出：【精确路径、格式、单位】，不要覆盖旧产物。
已知坑：【没有就写“无”】。交付前自查：【守恒/边界/量级/单位】。
卡面有错或做不下去就先停下，写进【疑点文档】，不要自己改卡。
完事后按线级 AGENTS.md §5「收工规矩」执行。
````

## 📋 独立复核方派发提示词（复制即用）
````text
请作为 T1 的独立复核方，只读检查执行方交付和本任务卡；不要修改被审文件和旧复核报告。
开工前报一下你的工具、模型、模型家族（家族按模型名写，连模型名都不知道才写 UNKNOWN）；论文结论/头条数字类任务，你与执行方同家族就在报告开头写明，不算通过。
复核对象：【输出路径、版本】。用和执行方不同的方法或来源核对关键数字：【写明】；两边对不上就记 UNKNOWN，不往下游放。
报告写到：【精确路径】。结论只能是：通过 / 有条件通过 / 退回 / UNKNOWN，并列出证据和未闭合条件。
决策登记表由主控登记，你只在 §3/§4 与 STATUS 记结论。
完事后按线级 AGENTS.md §5「收工规矩」执行。
````
