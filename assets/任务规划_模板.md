# 任务规划：<主题>

> 每张任务卡**自包含**——一个零上下文的 agent 只看这张卡就能动手，不用问。
> 同一项目里多张卡，用一致的编号（T1/T2…），在 AGENTS.md §4 看板里同步状态。

## T1 【任务名】
- **目标**：【一句话说清要产出什么】
- **谁负责**：【待填：数据 agent / 建模 agent / 用户】
- **⚙️ 建议模型**（可选）：【待填：Claude Sonnet 4 / GLM / DeepSeek / MIMO —— 适合本任务的模型。空着表示不限。注：长上下文读多文件优先 Claude；代码生成优先 DeepSeek；中文写作优先 GLM】
- **输入**（精确路径 / URL / DOI）：
  - 【待填：数据在 `手动下载/X/xxx.csv`】
  - 【待填：文献 DOI: 10.xxxx/xxxxx】
- **输出**（精确落盘）：
  - 【待填：`output/xxx.csv`（行数、列、单位）】
  - 【待填：`图/Fig_xxx.png`（分辨率、尺寸）】
- **环境**：【待填：base / dlcm env；需 PYTHONIOENCODING=utf-8？】
- **注意（坑 · 铁律）**：
  - 【待填：单位换算 g/kg → Mg/ha】
  - 【待填：口径必须 size_53µm，密度法不进主干】
  - 【待填：离散数据重采样用 near/mode】
- **验收**（可运行检查或可见产物）：
  - 【待填：`python check.py` 输出 0 错误 / 守恒偏差全 < 0.5%】
  - 【待填：图里 X/Y 轴有中文标题、无 SimHei 缺字方框】
- **依赖**：无 / 依赖 T0
- **已知风险**：【待填：某数据集有守恒偏差须标记；某 env 装包失败】
- **状态**：✅完成 2026-06-15 / ⏳进行中 / ⬜未开始
- **交付前必做**（逐条过完才算完成）：
  - [ ] 验收清单全过
  - [ ] （关键任务）独立复核通过（见 AGENTS.md §5「独立复核阀门」）
  - [ ] 更新 AGENTS.md §3（累计快照，带日期）+ §4 看板状态勾选
  - [ ] 写 STATUS.md（增量：本轮做了什么 / 动了哪些文件 / 踩了什么坑）
  - [ ] 关键数字一致性核对（同一数字各文档差 < 1%，见 §5）
  - [ ] 如有新脚本：更新 `scripts/README.md`；如有新数据：更新 `来源.txt`

## T2 【任务名】
（复制上面结构填写）

## 依赖关系图
```
T1 → T2 → T4
T3 与 T2 可并行
```

> ⚠️ **输出即输入**：上游任务的输出文件路径＝下游任务的输入引用。任务卡之间不是"描述依赖"就行了——下游的"输入"字段必须**精确引用上游输出的文件路径**。例如 T1 输出 `outputs/baseline/result.csv` → T2 输入写 `outputs/baseline/result.csv`，不是"T1 的结果"。
