# STATUS.md — 当前状态（最近一次交接）

> 读这个文件就能一秒接上当前工作，**不依赖对话历史**（对话可能被压缩/重置，文件不会）。
> 收工规矩：每个 agent 退出时必须更新此文件。AGENTS.md §3 只保留"最近 1–2 条摘要 + → 见 STATUS.md"。

## Handoff（YYYY-MM-DD，agent: xxx）

- **现状**：<1–3 条，当前正在干什么、干到哪了>
- **下一步**：<第一个动作是什么，精确到命令/文件路径>
- **未解决风险**：<下个 agent 需要注意的坑>
- **关键文件路径**：<3–5 个最重要的入口/数据/输出文件>
- **阻塞**：<需用户确认 X / 等数据下载 / 等外部依赖>
