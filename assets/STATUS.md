# STATUS.md — 增量 Handoff（最近一次交接）

> 这个文件**只记增量**：本 agent 这次做了什么、动了哪些文件、踩了什么坑。
> 全量累计快照看 AGENTS.md §3/§4；STATUS.md 不重复全量，只写"自上次以来的增量"。
> 收工规矩：每个 agent 退出时必须更新此文件。

## Handoff（YYYY-MM-DD，agent: xxx）

- **本次做了什么**：<本 agent 这一轮干了什么，1–3 条，动词开头。例：完成 T3 栅格重采样，输出 12 个 tif>
- **动了哪些文件**：<新增/修改/删除了哪些文件，精确路径。例：新建 `outputs/t3/*.tif`、更新 `scripts/reproject.py`>
- **关键数字变化**：<本轮新增/修订的关键数字。例：MAOC 均值从 4.2 → 4.18 g/kg（修正了单位换算 bug）>
- **踩了什么坑 / 警告下家**：<本 agent 遇到的坑或下个 agent 要注意的点。例：某栅格 NoData 值是 -9999 不是 0，别直接相加>
- **下一步建议**：<下个 agent 第一个动作，精确到命令/文件。例：先跑 `python scripts/check_t3.py` 验收>
- **阻塞**：<需用户确认 X / 等数据下载 / 等外部依赖。无则填"无">
