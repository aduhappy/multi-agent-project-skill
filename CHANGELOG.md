# Version notes

## 0.3.2 — 2026-09-19

来自一条论文线连续跑卡（L15–L17）暴露的缺口，每条只改一两行：

- 任务卡加状态 `🟠待裁定（不可派）`，并写明只有 `⬜未开始` 可派；执行方提示词第一句先查状态：此前执行方两次开工后才发现卡里问题未裁定。
- 家族按模型名写（GPT→OpenAI 等），连模型名都不知道才写 UNKNOWN：此前执行方连自己是 GPT 也写 UNKNOWN，连续三轮无法确认跨家族。论文结论/头条数字类复核写明不得与执行方同家族。
- 决策登记表只由主控登记：此前复核方收工时自行写入决策行。
- 卡面数字注明出处、执行方开工先核对：此前出卡方两次写错数字（分母用错、抽查脚本 bug），都靠执行方停工拦住。
- 任务卡加可选“判读规则”：结果出来前写死判据，产出先落候选文件。
- 模板开头的“自包含”改为明确边界：不依赖聊天记录，可读线级 AGENTS/STATUS 与卡内列出的输入。
- 减法：SKILL.md 删去重复的产物分级清单与重复案例；模板中 `git rev-parse` 身份红线标为仅多树/迁移项目保留。
- 检查器 A003 超限时提示归档做法。

## 0.3.1 — 2026-09-17

- 硬要求加第 10 条「哈希只记判据用得上的」：只给交付物、声称未改的输入、manifest 登记的关键产物记哈希；不给每份日志/快照逐个记，不套证据清单 + manifest + 自哈希。任务卡模板「验收」同步加一行。
- 起因：一张环境诊断任务卡多轮接力后堆出 19 处 SHA 要求，执行负担远大于诊断本身。

## 0.3.0 — 2026-09-17

在 GitHub 版（f94a590）基础上只加两样实用的东西，其余一律从简：

- **根索引 / 线级分开**：多线课题的根 `AGENTS.md` 只做索引（`assets/ROOT_AGENTS.md`），每条线自包含；检查器加 `--scope index`（I001–I003）。
- **任务卡双提示词**：关键任务卡同时带执行方和复核方两份派发提示词，并写明谁（工具/模型/家族）做的；不确定写 `UNKNOWN`。
- 检查器：H001–H009 结构检查 + A001–A004 提示 + 根索引 3 项，共 233 行；另有 `--today` 便于测试。
- 删除：委派任务模板、交付与复核报告模板、INDEX 对照表、仓库自身的 AGENTS/STATUS、测试夹具与布局检查脚本；英文镜像恢复为“待更新”短页。
- 测试：`tests/test_skill.py` 用临时目录现造根和线做冒烟测试。

曾经尝试过（T-MAP-FIX1–FIX4）让检查器解析任务卡表格、身份字段、文件指纹和状态措辞，四轮下来每轮都能找出新写法绕过或误报，最终整体删除。
